apiVersion: v1
kind: Service
metadata:
  name: {{ include "otacon.fullname" . }}
  labels:
    {{- include "otacon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "otacon.selectorLabels" . | nindent 4 }}
